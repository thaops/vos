import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color.fromARGB(255, 0, 169, 196);
  static const white = Colors.white;
  static const black = Colors.black;

  // Background colors
  static const lightBackground = Color(0xffF2F2F2);
  static const bacgroundApp = Color(0xffF3F9FF);
  static final backgroundError = Color(0xffF4F5FA);

  // Text colors
  static const grey = Color(0xffBEBEBE);
  static const colortextGray = Color.fromRGBO(113, 114, 122, 1);
  static const textSecondary = Color(0xFF9E9E9E);
  static const colorIcon = Color(0xff8F9098);

  // Task status colors
  static const colorBacklog = Color.fromRGBO(128, 128, 128, 1);
  static const inProgress = Color.fromRGBO(250, 140, 22, 1);
  static const colorDoing = Color.fromRGBO(20, 210, 3, 1);
  static const colorWaitingAndPending = Color.fromRGBO(138, 9, 95, 1);
  static const colorSpecification = Color.fromRGBO(224, 36, 199, 1);
  static const colorDevelopment = Color.fromRGBO(19, 194, 194, 1);
  static const colorInReview = Color.fromRGBO(5, 9, 141, 1);

  // Priority colors
  static const colorMedium = Color.fromRGBO(233, 200, 82, 1);
  static const colorNormal = Color.fromRGBO(17, 227, 28, 1);
  static const colorLow = Color.fromRGBO(128, 128, 128, 1);
}
